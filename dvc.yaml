stages:
  prepare:
    cmd: uv run python src/prepare.py data/dataset.csv data/prepared
    deps:
      - data/dataset.csv
      - src/prepare.py
    outs:
      - data/prepared

  train:
    cmd: uv run python src/train.py data/prepared data/models
    deps:
      - data/prepared
      - src/train.py
    outs:
      - data/models
